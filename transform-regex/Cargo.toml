[package]
name = "transform-regex"
version = "0.0.1"
edition = "2021"

[dependencies]
regex = "1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
depot-common = { path = "../depot-common" }
